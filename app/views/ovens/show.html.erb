<h3><%= @oven.name %></h3>

<div id="cookie-info" data-controller="oven-channel" data-oven-channel-id="<%= @oven.id %>">
  Cookie in oven:
  <% if @oven.cookies.present? %>
    <div id="cookie-list">
      <%= @oven.cookies.count %> cookies with <%= @oven.cookies.first.fillings.presence || "no fillings" %>
      <span id="cookie-baking-info">
        <% if @oven.cookies.all?(&:ready?) %>
        (Your Cookies are Ready)
        <% else %>
        (Your Cookies are being baked)
        <% end %>
      </span>
    </div>
    <%= button_to "Retrieve Cookie", empty_oven_path, class: 'button tiny' %>
  <% else %>
    None
  <% end %>
</div>

<br/>
<%= link_to "Prepare Cookie", new_oven_cookies_path(@oven), class: 'button' %>
